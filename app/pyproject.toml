[project]
name = "winch-control"
version = "0.1.0"
description = "BlueOS winch control extension"

dependencies = [
    "requests",
    "litestar",
    "uvicorn",
    "dynamixel-sdk",
    "pymavlink",
]

